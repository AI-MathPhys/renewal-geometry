# Gran-Tensor formalization triage — 2026-08-05

Full re-triage of all **123 remaining `conditional_interface` records** in
`manuscripts/Gran-Tensor/statements.json` (438 total: 177 proved, 138 encoded),
rated against the machinery currently in `NCG/` (waves 1–19).

**Tiers**

| Tier | Meaning | Count |
|---|---|---|
| **E** — easy | Direct with existing patterns; ≤150-line file | **7** |
| **M** — medium | Standard finite-dim work; one solid session each | **41** |
| **H** — hard-but-feasible | Needs a new (buildable) finite-dim mini-library | **39** |
| **B** — blocked | Mathlib-scale (AF/GNS/direct limits, continuum, BRST, lattice YM, analytic NT) or assembly over B-tier chains | **36** |

Each record's tier is also tagged in its ledger `note` as `TRIAGE <tier> (2026-08-05)`.

---

## Multiplier libraries (highest-leverage next steps)

Building these four mini-libraries would convert most of the H tier to M/E:

1. **Matrix square root / polar / pseudo-inverse-sqrt** (via `IsHermitian.spectral_theorem`;
   Mathlib pin has no `Matrix.PosSemidef.sqrt`). Unlocks **~12 records**:
   `lem:coherence-factorization`, `thm:coherence-parameterization`,
   `cth:marginals-no-relative-orientation`, `thm:universal-moment-leakage`,
   `thm:relative-metric-density`, `thm:common-action-router`,
   `thm:SM-occurrence-Choi-source`, `thm:SM-typed-occurrence-RN`,
   `thm:SMST-frame-transport`, `thm:common-CP-completion` (Julia block),
   `thm:locked-opportunity-instrument`, `lem:SMST-polar-edge` (already M, easier).
2. **Finite-dimensional double-commutant (bicommutant) theorem**. Unlocks
   `thm:SMST-polar-holonomy`, `cor:SMST-polar-extremes`, `thm:SMST-main-duality`,
   `thm:SMST-one-generation-audit`.
3. **SVD / singular-value Weyl perturbation**. Unlocks `thm:robust-hankel-gap`,
   `thm:store-block-decomposition` (and helps `thm:score-dwell-spectrum`).
4. **Gauss/Jacobi-sum estimates over 𝔽_q**. Unlocks `thm:ar-jacobi-edge` and feeds
   the carry-Bessel pair.

---

## Tier E — easy (7)

| Key | Content | Route |
|---|---|---|
| `thm:active-private-no-routing` | Active Reads annihilate old private contrasts; Gram vanishes | Composite-kernel + squared-norm on two-state source |
| `cor:uniform-store-gap` | Exponential dwell multiplier λ²/(λ²+ω²); gap (1−γ₀)ⁿ | One Laplace–cosine integral + monotonicity |
| `thm:record-dimension-obstruction` | Orthogonal nonzero point-Reads force carrier dim ≥ r_X | Short linear independence argument |
| `thm:global-record-saturation-obstruction` | Character eigenvectors ⊥ writer closure | Two-line inner-product/character argument |
| `thm:source-locality-propagation` | k-step off-filtration norm ≤ Σ one-step residuals | Telescoping induction, existing pattern |
| `thm:SM-anomaly-forced-weights` | Anomaly freedom iff 3a+2b=0; y=(1,4,−2,−3,−6,3) | Rational arithmetic; anomaly machinery exists |
| `cor:SM-literal-zero-occurrence` | Bures distance² = 65/32 for zero-occurrence Choi | Trace of explicit rational PSD matrix |

## Tier M — medium (41)

**Record/quotient cluster** (repo quotient-universality + finite algebra):
`thm:minimal-record`, `thm:minimal-read-algebra`, `thm:minimal-record-audit`,
`thm:record-refinement-bundle`, `thm:process-representation`,
`cor:causal-efficient-score-record`, `thm:renewal-semantic-stripping`,
`corollary:finite-recurrent-core-versus-growing-ledger`, `thm:ar-record-completion`.

**Gram/PSD/block cluster** (Schur residual, Douglas, fromBlocks):
`thm:joint-source-universality`, `thm:joint-source-cutoff-functor`,
`thm:coherence-parameterization` (once its H lemma lands),
`thm:sharp-positive-head-tail`, `thm:weighted-source-influence`,
`thm:common-predictor-innovation`, `thm:GRH-completed-Euler-short`,
`thm:GRH-block-first-bridge`.

**Renewal/Hankel/feedback cluster** (telescopes, Hankel, block memory):
`thm:predictive-alternative`, `thm:accepted-response-renewal`,
`thm:universal-feedback-memory`, `thm:flat-alphabet-bound`,
`cth:prefix-support-memory`, `thm:ordered-memory`,
`thm:generator-native-renewal`, `thm:connected-residual-factorization`.

**SM/SMST finite-algebra cluster**:
`thm:SMST-phase-grading-route`, `thm:SMST-monoidal-CAR`, `thm:SM-generation`,
`thm:SM-top-Walsh`, `thm:SM-six-state-no-matter`, `thm:SM-cross-support-generator`,
`cth:SM-occurrence-marginal-nonselection`, `cor:SM-occurrence-environment-rank`,
`lem:SMST-polar-edge`, `cor:SMST-polar-extremes` (needs bicommutant first).

**Arithmetic/duality cluster**:
`thm:ar-finite-Euler`, `thm:ar-add-mult`, `thm:Smith-Pontryagin`,
`corollary:closed-spin-networks-separate-gauge-orbits` (after `thm:spin-network`).

**Concrete singletons**: `thm:fresh-endpoint-retained-record` (Helstrom (5+2√6)/10),
`cor:acceptance-likelihood`.

## Tier H — hard-but-feasible (39)

- **Sqrt/polar-blocked** (see multiplier #1): `lem:coherence-factorization`,
  `cth:marginals-no-relative-orientation`, `thm:universal-moment-leakage`,
  `thm:relative-metric-density`, `thm:common-action-router`,
  `thm:SM-occurrence-Choi-source`, `thm:SM-typed-occurrence-RN`,
  `thm:SMST-frame-transport`, `thm:common-CP-completion`,
  `thm:locked-opportunity-instrument`, `thm:SMST-store-native-route`,
  `thm:universal-Calderon-frame` (also needs exp/integral).
- **Bicommutant-blocked**: `thm:SMST-polar-holonomy`, `thm:SMST-main-duality`,
  `thm:SMST-one-generation-audit`.
- **SVD/Weyl-blocked**: `thm:robust-hankel-gap`, `thm:store-block-decomposition`,
  `thm:score-dwell-spectrum`.
- **Uniqueness-unitary/Stinespring chains**: `thm:comb-tomography`,
  `thm:canonical-comb-dilation`, `thm:operational-record-completion`,
  `thm:SM-autonomous-test`, `thm:SM-completion-conservative`.
- **New finite libraries**: `cor:canonical-full-matrix-control` (su(4) bracket closure),
  `thm:SM-group` (S(U(3)×U(2))/Z₆), `thm:SMST-record-native-generations` (isotypic Grams),
  `thm:SMST-generator-projections`, `thm:SMST-action-rigidity`,
  `thm:universal-Hankel-exhaustion` (Krylov), `thm:CP-memory-compactness`,
  `thm:GRH-Petz-Euler` (finite modular/Petz), `thm:spin-network` (finite Peter–Weyl),
  `thm:chain-Pontryagin` (homological mini-library), `cor:source-core-Gram-transport`,
  `thm:ar-jacobi-edge`, `thm:ar-carry-Bessel`, `thm:ar-pair-quotient-Bessel`.
- **Large finite assemblies**: `thm:loaded-subhierarchy`, `thm:arithmetic-loading`.

## Tier B — blocked (36)

- **AF/GNS/direct-limit/quasilocal** (mathlib-scale): `thm:AF-limit-state`,
  `thm:projective-state-compactness`, `thm:joint-source-inductive-limit`,
  `thm:quasilocal-completion`, `thm:compatible-future-forms`,
  `thm:GNS-observable-limit`, `thm:primitive-writer-saturation`,
  `thm:state-compactness-tightness`, `thm:universal-coercive-continuum`,
  `thm:universal-physical-continuum`, `thm:uniform-gap-limit`.
- **Continuum geometry/gauge** (BRST, Einstein, line bundles, Palatini):
  `thm:SMST-ADM-Cartan`, `thm:finite-HDA`, `thm:SMST-finite-action-Einstein`,
  `thm:Palatini`, `thm:SMST-graph-regulator`, `cor:SMST-graph-massless`,
  `thm:SMST-line-valued-Einstein`, `thm:SMST-corrected-dual-measure`.
- **Lattice YM / Haar analysis**: `thm:YM-primitive-loop-saturation`,
  `thm:YM-strong-electric-gap`, `thm:SM-colour-orbit`.
- **Analytic number theory beyond finite**: `thm:ar-fixed-order-source`,
  `thm:ar-three-match`, `thm:ar-few-orbit`, `thm:ar-PCJM-handoff`.
- **Other**: `thm:source-core-semigroup` (C₀-semigroups),
  `thm:central-source-decomposition` (C*-Artin–Wedderburn),
  `thm:canonical-history-compiler` (su(d) simplicity), `thm:anytime-audit`
  (anytime martingale bounds), `thm:primitive-phase-selection` (unprinted data).
- **Omnibus assemblies over B-chains**: `thm:master-architecture`,
  `thm:grand-emergence`, `thm:SM-GT`, `thm:SMST-loaded-emergence`,
  `thm:integrated-grand-tensor`.

---

## Recommended wave plan

- **Wave 20**: the 7 E records + start the **matrix sqrt/polar mini-library**.
- **Waves 21–23**: sqrt-unlocked coherence/moment/RN cluster (~12 records),
  then the M-tier record/quotient and Gram clusters (~17 records).
- **Wave 24+**: bicommutant library → SMST commutant cluster; SVD/Weyl → Hankel gap;
  renewal/Hankel M cluster; arithmetic M cluster.
- **Long tail**: H assemblies (`loaded-subhierarchy`, `arithmetic-loading`) once their
  cited finite pieces are all proved; B tier stays `conditional_interface` by policy.
