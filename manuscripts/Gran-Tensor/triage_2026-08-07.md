# Gran-Tensor formalization triage — 2026-08-07

Fresh triage of the **80 remaining `conditional_interface` records** in
`manuscripts/Gran-Tensor/statements.json`.

**Ledger state**: 651 records = **368 proved · 203 statement_encoded · 80 conditional**.
The 2026-08-05 triage (123 conditionals at 438 records) has been fully worked
off; every one of the current 80 comes from chapters added since, so none
carries an earlier tier tag.

**Ledger hygiene flag**: two keys have no matching `\label` in the tex —
`corollary:action-derived-scalar-current` and
`corollary:common-action-provenance-discharges-the-normal-endpoint-comparison`
(the tex almost certainly uses `cor:`-prefixed labels). Fix the keys before the
next coverage run touches them.

**Tiers** (rated against current `NCG/` machinery: finite matrix/CP algebra,
Kronecker eigen, Krylov/Submodule, HasDerivAt calculus, group averaging,
K4/Walsh banks, Perron/IFT jets, Schur tests, decide):

| Tier | Meaning | Count |
|---|---|---|
| **E** | Direct with existing patterns; one short file | **10** |
| **M** | Standard finite-dimensional work; one solid session | **26** |
| **H** | Needs a new (buildable) mini-library or is a large finite computation | **24** |
| **B** | Mathlib-scale: AF/C*-inductive limits, GNS limits, Mosco, process/SPDE convergence, lattice-YM measures, analytic-NT compilers | **18** |
| (mismatch) | ledger-key/label fix needed first | 2 |

---

## Tier E — easy (10)

| Key | Content / route |
|---|---|
| `thm:central-source-decomposition` | Central idempotent Peirce split `A = zA ⊕ (1−z)A`; abstract ring algebra |
| `thm:renewal-continuous-completion` | LST `8λ²/((4λ+5s)(2λ+3s))`, mean `11/(4λ)`; rational algebra (distributional identity as LST) |
| `thm:accepted-response-pressure` | Pressure solves a quadratic; verify the boxed root `(−8+√(64+60A))/(2A)`; sqrt algebra |
| `thm:accepted-phase-feedback` | Explicit rational `2×2` phase–payload blocks `A_θ, B_θ, C_θ`; entrywise computation |
| `thm:concrete-renewal-continuum-profile` | `M₀φ = −(7/15)φ`, `Var = 30/121`, Walsh/Kronecker eigenvalues via existing `kronecker_eigen` |
| `thm:phase-invariant-axis` | Orthogonal split of `1ᵀ⊥ ⊂ ℝ^{n+1}` + Pythagoras norm identity; Finset inner products |
| `thm:primitive-writer-saturation` | Norm-dense subalgebra ⇒ equal cyclic closures; continuity of the orbit map |
| `thm:uniform-gap-limit` | `‖Q∞T∞Q∞‖ = sup_X ρ_X` rendered on the dense union of nested cutoff subspaces |
| `cth:second-moment-nontriviality` | Explicit witness `μ_n = (1−1/n²)δ₀ + (1/2n²)(δ_n + δ_{−n})`; finite-support integrals |
| `cor:endpoint-defect-symmetry` | Isotypic block form `C = ⊕ I⊗C_π` with Schur displayed; irreducible case scalar |

## Tier M — medium (26)

**Renewal/process cluster**: `thm:source-core-semigroup` (Duhamel/resolvent
defect algebra — BKM pattern), `thm:fresh-opportunity-invisibility` (averaged
branch factorization), `cth:fixed-delay-hidden-generator` (explicit 3×3
rotation-generator witness `e^{tQ⁰}=e^{tQ¹}`), `cth:private-fingerprint-no-provenance-channel`
(one-way block-dilation construction), `cor:primitive-score-continuum`
(martingale Gram + Schur innovation `τG₀`; Donsker limit disclosed),
`thm:concrete-renewal-score-profile` (K4 tetrahedral bank — repo K4 machinery),
`thm:continuum-memory-completion` (Volterra ⇔ block ODE + Kalman quotient —
stineStack machinery), `thm:fast-feedback-collapse` (discrete Grönwall
induction), `thm:reward-pressure-reconstruction`, `thm:reward-pressure-jet`
(implicit-function pressure jets — PerronJet pattern).

**Limit-scaffolding (finite renderings)**: `thm:GNS-observable-limit`
(dense-domain BLT extension), `thm:projective-state-compactness` (diagonal
extraction over finite stages), `thm:global-cylinder-descent` (finite clauses:
polar isometries compose, predictor rank gain), `thm:compatible-future-forms`
(bounded-action criterion on dense union), `thm:universal-coercive-continuum`
(2×2 head–tail numerical-range bound `q*`), `thm:gap-soft-mode-alternative`
(liminf gap passage on fixed vectors), `thm:state-compactness-tightness`
(Chebyshev tightness; Alaoglu part), `thm:anytime-audit` (union bound over
`π²k²/3` weights with per-count Hoeffding displayed).

**SM/SMST finite cluster**: `thm:SM-colour-orbit` (Schur-orthogonality Haar
average displayed; rational projector arithmetic `Π₁ + (1/5)Π₁₅`),
`thm:SMST-support-polar-commutant` (finite commutant/functional calculus),
`thm:SMST-quiver-commutant` (HS commutator form, kernel = `End 𝔔` —
ConnectedGram pattern), `thm:SMST-graph-regulator` (`(H_m^±)²` block identity +
spectral gap `m`), `cor:SMST-graph-massless` (massless limit of the graph
connection on constant-rank branch), `thm:SMST-corrected-dual-measure`
(conjugate/transpose sign table), `thm:canonical-history-compiler` (controlled
`su(d)` lift — qutrit/Pauli machinery, partially displayed),
`thm:primitive-phase-selection` (25-cell lock uniqueness up to the listed
coordinate changes — finite but fiddly).

## Tier H — hard-but-feasible (24)

**Mega-assemblies** (ClockDischarge-style, but must wait for constituents;
several constituents are B): `thm:master-architecture`, `thm:grand-emergence`,
`thm:integrated-grand-tensor`, `thm:SM-GT`, `thm:SMST-loaded-emergence`,
`thm:SMST-coercive-multiplicity-closure`.

**Discrete-geometry mini-libraries**: `thm:pressure-flow-isoperimetry`
(max-flow/min-cut + congestion duality), `thm:operational-Sobolev-Weyl`
(discrete co-area/Sobolev from cut inequality), `thm:coordinate-atlas-isoperimetry`
(Loomis–Whitney axis bound), `thm:entropic-Hodge-continuum` (Fisher-window
Hessian is M; minimizing movement is the hard half).

**Large finite computations**: `thm:finite-HDA` (full finite bracket closure),
`thm:SMST-finite-action-Einstein` (finite Ward/BRST/stress variational
algebra), `thm:renewal-sixth-order-cell-profile` (Stieltjes jet through 6th
order), `thm:renewal-chaos-localization` (number-operator conjugation bound),
`thm:renewal-memory-cumulant` (cq log-partition Hessian — BKM reachable),
`thm:YM-strong-electric-gap` (the boxed 2×2 root bound is our Schur-test `q*`
shape — M core, model layer H), `thm:YM-primitive-loop-saturation` (lattice
gauge density).

**Analysis-layer packets**: `thm:SMST-ADM-Cartan`, `thm:SMST-regenerative-ADM`
(Hölder/`W^{1,p}` compactness packets), `thm:Palatini` (weak-`L^p` insertion),
`thm:SMST-global-boundary` (Stokes/corner first variation),
`thm:SMST-line-valued-Einstein` (phase-line curvature/holonomy),
`thm:ar-three-match` (12-atom matching combinatorics — nearest to M of the
arithmetic four).

## Tier B — blocked at Mathlib scale (18)

C*-inductive limits / AF / GNS-limit semantics: `thm:AF-limit-state`,
`thm:joint-source-inductive-limit`, `thm:quasilocal-completion`.
Mosco/variational convergence: `thm:GT-Mosco`, `thm:compact-screen-alternative`,
`thm:SMST-continuum-Howe`, `thm:periodic-covariant-information-limit`
(norm-resolvent Fourier interpolation — the least blocked; H⁻ if attacked).
Process/SPDE limits: `thm:renewal-interchange-OU` (OU field in `D([0,T],𝒟')`),
`thm:renewal-regenerative-continuum`, `cor:renewal-positive-continuum`,
`thm:universal-physical-continuum`, `thm:GT-universal-limit-alternative`
(assemblies whose clauses are the above), `cor:renewal-spatial-positive-screen`,
`cor:SMST-regenerative-screen`, `cor:protected-ledger-short`,
`cor:finite-resolution-memory` (assemblies over B/H inputs).
Analytic NT compilers: `thm:ar-fixed-order-source` (Vaughan–Heath-Brown order-12
compiler), `thm:ar-few-orbit` (character-orbit `L²` with Gauss sums),
`thm:ar-PCJM-handoff` (assembly over both).

---

**Recommended attack order**: E-tier sweep (one commit, ~10 records) → the
M-tier renewal/process cluster and SMST finite cluster (each maps onto an
existing repo pattern) → the three limit-scaffolding files → mini-libraries
(flow/min-cut first: it unlocks three H records) → mega-assemblies last, in
ClockDischarge style, with B-tier clauses as displayed hypotheses.
